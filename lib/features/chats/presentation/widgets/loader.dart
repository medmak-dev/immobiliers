import 'package:flutter/material.dart';

class CircularLoader extends CircularProgressIndicator {
  const CircularLoader({Key? key})
      : super(
          key: key,
          color: Colors.black,
          strokeWidth: 2.0,
        );
}
